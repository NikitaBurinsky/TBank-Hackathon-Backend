# 🏗 TBank Hackathon Backend: Scalable Microservices Architecture

> **Высокопроизводительный бэкенд, реализующий паттерны микросервисной архитектуры с использованием гибридного стека .NET 8 и Python.**

Проект представляет собой распределенную систему для обработки сложных запросов планирования и анализа данных. Архитектура построена на принципах **Service-Oriented Architecture (SOA)** с четким разделением ответственности между сервисами и использованием **gRPC** для высокоскоростного межсервисного взаимодействия.

---

## 📐 Архитектурный обзор (System Design)

Система спроектирована как набор изолированных контейнеризированных сервисов, объединенных в единую сеть.

### Компоненты системы
1.  **Gateway & Proxy (Nginx)**: Единая точка входа. Терминирует SSL, раздает статику и балансирует трафик на основной API-шлюз.
2.  **Core Service (ASP.NET Core)**: Основной микросервис-оркестратор. Отвечает за бизнес-логику, аутентификацию (Identity), управление данными и взаимодействие с клиентом по REST API.
3.  **Compute/Worker Service (Python)**: Специализированный микросервис для выполнения ресурсоемких вычислительных задач и обработки неструктурированных данных. Работает в асинхронном режиме.
4.  **Database (PostgreSQL)**: Реляционное хранилище данных, управляемое через EF Core Migrations.

### 🔄 Inter-Service Communication
Вместо традиционного HTTP/REST для общения между бэкенд-сервисами используется **gRPC (Google Remote Procedure Call)**.
* **Protocol Buffers (.proto)**: Строгая типизация контрактов между C# и Python.
* **Performance**: Бинарная сериализация и HTTP/2 обеспечивают минимальную задержку при передаче данных между оркестратором и воркером.

---

## 🛠 Технический стек

### Infrastructure & DevOps
* **Docker & Docker Compose**: Полная изоляция окружения. Описание инфраструктуры в коде (IaC).
* **Nginx**: Reverse proxy, конфигурация маршрутизации.
* **PostgreSQL 15**: Основная СУБД.

### Backend Core (Service A)
* **Platform**: .NET 8 (C#)
* **Framework**: ASP.NET Core Web API
* **ORM**: Entity Framework Core (Code-First approach).
* **Auth**: ASP.NET Core Identity (Role-based security).
* **Logging**: Serilog (структурированное логирование в файлы и консоль).
* **API Documentation**: Swagger/OpenAPI.

### Computational Service (Service B)
* **Language**: Python
* **Framework**: gRPC (AsyncIO).
* **Concurrency**: Асинхронная модель обработки запросов (`asyncio`) для высокой пропускной способности.

---

## ⚙️ Ключевые инженерные решения

### 1. Гибридная микросервисная среда
Интеграция двух разных экосистем (.NET и Python) в единый пайплайн.
* **Зачем**: Использование сильных сторон каждого языка. C# обеспечивает надежность, типизацию и скорость бизнес-логики. Python берет на себя специфические задачи обработки данных.
* **Реализация**: `Micser-ASP` выступает клиентом, создавая gRPC-канал к `Micser-Python`.

### 2. Стохастическое планирование
Реализован сложный алгоритм `PlannerService` для составления планов питания. Алгоритм работает с многокритериальной оптимизацией, подбирая комбинации продуктов под динамические целевые метрики (КБЖУ) пользователя.

### 3. Надежность и безопасность
* **CORS Policy**: Строгая настройка политик Cross-Origin для защиты от несанкционированных запросов с фронтенда.
* **Identity Management**: Кастомная настройка `UserManager` и `SignInManager` с поддержкой ролевой модели доступа.
* **Health Checks**: Конфигурация healthcheck-ов для базы данных в Docker Compose для обеспечения корректного порядка запуска сервисов (`depends_on`).

---

## 🚀 Запуск проекта (Deploy)

Проект полностью готов к развертыванию через контейнеры. Не требует установки SDK локально.

```bash
# Сборка и запуск всех микросервисов в фоновом режиме
docker-compose up -d --build
